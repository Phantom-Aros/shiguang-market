{
  "name": "@shiguang/icons",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "node scripts/generate-subset.mjs",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "prebuild": "npm run generate",
    "prelint": "npm run generate"
  },
  "dependencies": {
    "@iconify-json/mdi-light": "^1.2.2",
    "@iconify/types": "^2.0.0",
    "@iconify/utils": "^3.1.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
